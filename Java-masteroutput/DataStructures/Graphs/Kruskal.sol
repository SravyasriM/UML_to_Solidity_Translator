pragma solidity 0.7.5;

import "java/util/Comparator.sol";

import "java/util/HashSet.sol";

import "java/util/PriorityQueue.sol";

contract Kruskal
{
}

library Kruskal_Library
{
	function main(string[] memory args) public
	{
			for(int32 i = int32(0); i < graph.length; i++;)
			{
					;
			}
			addEdge(graph, 0, 1, 2);
			addEdge(graph, 0, 2, 3);
			addEdge(graph, 0, 3, 3);
			addEdge(graph, 1, 2, 4);
			addEdge(graph, 2, 3, 5);
			addEdge(graph, 1, 4, 3);
			addEdge(graph, 2, 4, 1);
			addEdge(graph, 3, 5, 7);
			addEdge(graph, 4, 5, 8);
			addEdge(graph, 5, 6, 9);
			System.out.println("Initial Graph: ");
			for(int32 i = int32(0); i < graph.length; i++;)
			{
			}
			Kruskal k;
			System.out.println("\nMinimal Graph: ");
			for(int32 i = int32(0); i < solGraph.length; i++;)
			{
			}
	}

}

//Optional.empty


//Optional.empty


//Optional.empty


//Optional.empty


//Optional.empty


//Optional.empty


//Optional.empty


//Optional.empty


//Optional.empty


