pragma solidity 0.7.5;

//package strings


contract CharactersSame
{
	/**
 * check if all the characters of a string are same
 *
 * @param s the string to check
 * @return {@code true} if all characters of a string are same, otherwise {@code false}
 */

	function isAllCharactersSame(string s) public returns(bool)
	{
	}

}

library CharactersSame_Library
{
	/**
 * Driver Code
 */

	function main(string[] memory args) public
	{
	}

	/**
 * check if all the characters of a string are same
 *
 * @param s the string to check
 * @return {@code true} if all characters of a string are same, otherwise {@code false}
 */

	function isAllCharactersSame(string s) public returns(bool)
	{
	}

}

