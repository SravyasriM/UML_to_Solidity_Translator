pragma solidity 0.7.5;

//package strings


/**
 * Wikipedia: https://en.wikipedia.org/wiki/Palindrome
 */

contract Palindrome
{
	/**
 * Check if a string is palindrome string or not
 *
 * @param s a string to check
 * @return {@code true} if given string is palindrome, otherwise {@code false}
 */

	function isPalindrome(string s) public returns(bool)
	{
	}

	/**
 * Check if a string is palindrome string or not using recursion
 *
 * @param s a string to check
 * @return {@code true} if given string is palindrome, otherwise {@code false}
 */

	function isPalindromeRecursion(string s) public returns(bool)
	{
	}

	/**
 * Check if a string is palindrome string or not another way
 *
 * @param s a string to check
 * @return {@code true} if given string is palindrome, otherwise {@code false}
 */

	function isPalindrome1(string s) public returns(bool)
	{
	}

}

library Palindrome_Library
{
	/**
 * Driver Code
 */

	function main(string[] memory args) public
	{
	}

	/**
 * Check if a string is palindrome string or not
 *
 * @param s a string to check
 * @return {@code true} if given string is palindrome, otherwise {@code false}
 */

	function isPalindrome(string s) public returns(bool)
	{
	}

	/**
 * Check if a string is palindrome string or not using recursion
 *
 * @param s a string to check
 * @return {@code true} if given string is palindrome, otherwise {@code false}
 */

	function isPalindromeRecursion(string s) public returns(bool)
	{
	}

	/**
 * Check if a string is palindrome string or not another way
 *
 * @param s a string to check
 * @return {@code true} if given string is palindrome, otherwise {@code false}
 */

	function isPalindrome1(string s) public returns(bool)
	{
	}

}

