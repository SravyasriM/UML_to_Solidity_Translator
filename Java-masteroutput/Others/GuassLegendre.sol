pragma solidity 0.7.5;

//package Others


/**
 * Guass Legendre Algorithm ref https://en.wikipedia.org/wiki/Gauss–Legendre_algorithm
 *
 * @author AKS1996
 */

contract GuassLegendre
{
	function pi(int32 l) internal returns(int64)
	{
			int64 a = int64(1); //Converted from DOUBLE TYPE of Java
			for(int32 i = int32(0); i < l; ++i;)
			{
					a = temp[uint256(0)];
					b = temp[uint256(1)];
					t = temp[uint256(2)];
					p = temp[uint256(3)];
			}
			return ;
	}

	function update(int64 a, int64 b, int64 t, int64 p) internal returns(

}

library GuassLegendre_Library
{
	function main(string[] memory args) public
	{
			for(int32 i = int32(1); i <= 3; ++i;)
			{
				System.out.println(pi(i));			}
	}

	function pi(int32 l) internal returns(int64)
	{
			int64 a = int64(1); //Converted from DOUBLE TYPE of Java
			for(int32 i = int32(0); i < l; ++i;)
			{
					a = temp[uint256(0)];
					b = temp[uint256(1)];
					t = temp[uint256(2)];
					p = temp[uint256(3)];
			}
			return ;
	}

	function update(int64 a, int64 b, int64 t, int64 p) internal returns(

}

