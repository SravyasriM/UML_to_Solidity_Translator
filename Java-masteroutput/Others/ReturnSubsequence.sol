pragma solidity 0.7.5;

//package Others


import "java/util/Scanner.sol";

contract ReturnSubsequence
{
	/**
 * @param givenString
 * @return subsequence
 */

	function returnSubsequence(string givenString) private returns(

}

library ReturnSubsequence_Library
{
	function main(string[] memory args) public
	{
			System.out.println("Enter String: ");
			Scanner s;
			// given string

			string memory givenString = string(s.next());
			System.out.println("Subsequences : ");
			// print the given array of subsequences

			for(int32 i = int32(0); i < subsequence.length; i++;)
			{
					System.out.println(subsequence[uint256(i)]);
			}
			s.close();
	}

	/**
 * @param givenString
 * @return subsequence
 */

	function returnSubsequence(string givenString) private returns(

}

